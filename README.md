
  # Website Default State (Copy)

  This is a code bundle for Website Default State (Copy). The original project is available at https://www.figma.com/design/mjMkPkqEeDKURFko8xRMI4/Website-Default-State--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  